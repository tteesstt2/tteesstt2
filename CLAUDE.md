# 프로젝트: 유튜브 여행 영상 장소·장면 검색

유튜브 일본 여행 브이로그를 Gemini로 분석해 장소·장면을 추출하고, 관리자가 검증한 뒤, 구글처럼 검색할 수 있게 하는 서비스. 현재는 설계·조사 단계이며 코드는 아직 없다.

**작업을 시작하기 전에 `docs/handoff.md`를 먼저 읽는다.** 지금까지의 결정, 열린 질문, 환경 설정 상태, 다음 할 일이 정리되어 있다.

- 사용자와는 한국어로, 쉬운 말로 대화한다.
- 추출은 영상에서 확인되는 사실 위주. 근거 없는 장소명은 저장하지 않는다.
- 스크래핑 금지. 공식 API와 공개 데이터만 사용한다.
- API 키는 채팅으로 받지 않는다. 환경 변수(`YOUTUBE_API_KEY`, `NAVER_CLIENT_ID`, `NAVER_CLIENT_SECRET`, Gemini 키)로 받는다.
- 결정이 바뀌거나 작업이 진행되면 `docs/handoff.md`를 갱신한다.
